<div class="main-container">
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-4 offset-md-1">
                    <div class="hero-content">
                        <h1 class="hero-title animate__animated animate__fadeInDown">QuerySync</h1>
                        <p class="hero-subtitle animate__animated animate__fadeInUp">Connect. Learn. Grow.</p>
                        <p class="hero-description animate__animated animate__fadeInUp">Post questions and get your answers from experienced alumni.</p>
                        <div class="hero-buttons">
                            <a href="/register" class="btn btn-primary btn-lg animate__animated animate__fadeInLeft">Get Started</a>
                            <a href="/login" class="btn btn-outline-light btn-lg animate__animated animate__fadeInRight">Login</a>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="hero-image-container">
                        <img src="/hero.png" alt="Students and Alumni Connecting" class="hero-image">
                        <div class="floating-elements">
                            <div class="floating-element element-1">
                                <i class="fas fa-comments"></i>
                            </div>
                            <div class="floating-element element-2">
                                <i class="fas fa-lightbulb"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Grid -->
    <div class="features-grid">
        <div class="feature-card" data-aos="fade-up" data-aos-delay="100">
            <div class="feature-icon">
                <i class="fas fa-graduation-cap"></i>
            </div>
            <h3>Connect with Alumni</h3>
            <p>Build your professional network</p>
        </div>
        <div class="feature-card" data-aos="fade-up" data-aos-delay="200">
            <div class="feature-icon">
                <i class="fas fa-comments"></i>
            </div>
            <h3>Ask Questions</h3>
            <p>Get expert advice</p>
        </div>
        <div class="feature-card" data-aos="fade-up" data-aos-delay="300">
            <div class="feature-icon">
                <i class="fas fa-calendar-alt"></i>
            </div>
            <h3>Join Events</h3>
            <p>Network & Learn</p>
        </div>
    </div>

    <!-- CTA Section -->
    <div class="cta-section" data-aos="zoom-in">
        <div class="cta-content">
            <h2>Ready to Start Your Journey?</h2>
            <a href="/register" class="btn btn-primary btn-lg">Join Now</a>
        </div>
    </div>
</div>

<style>
    /* Main Container */
    .main-container {
        min-height: 100vh;
        background: linear-gradient(135deg, #1a1a2e, #16213e);
        color: white;
        overflow: hidden;
    }

    /* Hero Section */
    .hero-section {
        height: 100vh;
        display: flex;
        align-items: center;
        position: relative;
        overflow: hidden;
    }

    .hero-content {
        text-align: left;
        z-index: 2;
        padding: 0;
        margin-top: -2rem;
    }

    .hero-title {
        font-size: 4.5rem;
        font-weight: 800;
        margin-bottom: 1rem;
        background: linear-gradient(45deg, #00dbde, #fc00ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 20px rgba(0, 219, 222, 0.3);
    }

    .hero-subtitle {
        font-size: 1.8rem;
        margin-bottom: 2rem;
        opacity: 0.9;
    }

    .hero-description {
        font-size: 1.2rem;
        margin-bottom: 2rem;
        opacity: 0.8;
        color: #fff;
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        justify-content: flex-start;
    }

    .btn-primary {
        background: linear-gradient(45deg, #00dbde, #fc00ff);
        border: none;
        padding: 1rem 2rem;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 219, 222, 0.3);
    }

    .btn-outline-light {
        border: 2px solid white;
        padding: 1rem 2rem;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-outline-light:hover {
        background: white;
        color: #1a1a2e;
        transform: translateY(-5px);
    }

    .hero-image-container {
        position: relative;
        height: 100%;
        min-height: 400px;
        display: flex;
        align-items: center;
        justify-content: flex-end;
        padding-right: 2rem;
    }

    .hero-image {
        width: 100%;
        max-width: 600px;
        height: auto;
        animation: float 6s ease-in-out infinite;
        filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.15));
        margin-left: 3rem;
    }

    .floating-elements {
        display: none; /* Hiding the floating elements since the illustration has its own elements */
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    @media (max-width: 991.98px) {
        .hero-section {
            text-align: center;
            padding: 3rem 0;
            height: auto;
        }

        .hero-content {
            text-align: center;
            margin-top: 0;
        }

        .hero-image {
            max-width: 400px;
            margin: 0 auto;
        }

        .hero-image-container {
            margin-top: 2rem;
            min-height: 300px;
            justify-content: center;
            padding-right: 0;
        }

        .hero-title {
            font-size: 3rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
        }

        .hero-description {
            font-size: 1rem;
        }

        .hero-buttons {
            justify-content: center;
        }
    }

    /* Features Grid */
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        padding: 4rem 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 2rem;
        text-align: center;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .feature-card:hover {
        transform: translateY(-10px);
        background: rgba(255, 255, 255, 0.15);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
        background: linear-gradient(45deg, #00dbde, #fc00ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    /* CTA Section */
    .cta-section {
        padding: 4rem 2rem;
        text-align: center;
    }

    .cta-content {
        max-width: 600px;
        margin: 0 auto;
    }

    .cta-content h2 {
        font-size: 2.5rem;
        margin-bottom: 2rem;
        background: linear-gradient(45deg, #00dbde, #fc00ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
</style>

<!-- Add AOS (Animate On Scroll) Library -->
<link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>
    AOS.init({
        duration: 1000,
        once: true
    });
</script>
